apiVersion: v1
kind: Service
metadata:
  name: rtmp-service
  annotations:
    external-dns.alpha.kubernetes.io/hostname: rtmp.rizkyaddon.com
spec:
  type: LoadBalancer
  selector:
    app: rtmp-server
  ports:
  - protocol: TCP
    port: 1935
    targetPort: 1935